services:
  catalogdb:
    environment:
      POSTGRES_DB: CatalogDb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    restart: always
    ports:
      - "5433:5432"
    volumes:
      - postgres_catalog:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d CatalogDb"]
      interval: 5s
      timeout: 5s
      retries: 10

  basketdb:
    environment:
      POSTGRES_DB: BasketDb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    restart: always
    ports:
      - "5434:5432"
    volumes:
      - postgres_basket:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d BasketDb"]
      interval: 5s
      timeout: 5s
      retries: 10

  distributedcache:
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  identitydb:
    environment:
      POSTGRES_DB: IdentityDb
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    restart: always
    ports:
      - "5435:5432"
    volumes:
      - postgres_identity:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d IdentityDb"]
      interval: 5s
      timeout: 5s
      retries: 10

  catalog.api:
    image: catalog-api
    build:
      context: .
      dockerfile: Catalog.AP/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_HTTP_PORTS: 8080
      ConnectionStrings__Database: "Server=catalogdb;Port=5432;Database=CatalogDb;User Id=postgres;Password=postgres;Include Error Detail=true"
      Jwt__Issuer: ${JWT_ISSUER:-eshop.identity}
      Jwt__Audience: ${JWT_AUDIENCE:-eshop.apis}
      Jwt__Key: ${JWT_KEY}
      Cors__AllowedOrigins__0: http://localhost:5173
      Cors__AllowedOrigins__1: http://localhost:8088
    ports:
      - "6002:8080"
    depends_on:
      catalogdb:
        condition: service_healthy

  basket.api:
    image: basket-api
    build:
      context: .
      dockerfile: Basket/Basket/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_HTTP_PORTS: 8080
      ConnectionStrings__Database: "Server=basketdb;Port=5432;Database=BasketDb;User Id=postgres;Password=postgres;Include Error Detail=true"
      ConnectionStrings__Redis: "distributedcache:6379,abortConnect=false"
      Jwt__Issuer: ${JWT_ISSUER:-eshop.identity}
      Jwt__Audience: ${JWT_AUDIENCE:-eshop.apis}
      Jwt__Key: ${JWT_KEY}
      Cors__AllowedOrigins__0: http://localhost:5173
      Cors__AllowedOrigins__1: http://localhost:8088
    ports:
      - "6001:8080"
    depends_on:
      basketdb:
        condition: service_healthy
      distributedcache:
        condition: service_healthy

  identity.api:
    image: identity-api
    build:
      context: .
      dockerfile: Identity/Identity.API/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_HTTP_PORTS: 8080
      ConnectionStrings__IdentityDatabase: "Server=identitydb;Port=5432;Database=IdentityDb;User Id=${POSTGRES_USER:-postgres};Password=${POSTGRES_PASSWORD:-postgres};Include Error Detail=true"
      Jwt__Issuer: ${JWT_ISSUER:-eshop.identity}
      Jwt__Audience: ${JWT_AUDIENCE:-eshop.apis}
      Jwt__Key: ${JWT_KEY}
      Jwt__AccessTokenMinutes: ${JWT_ACCESS_TOKEN_MINUTES:-15}
      Jwt__RefreshTokenDays: ${JWT_REFRESH_TOKEN_DAYS:-7}
      Cors__AllowedOrigins__0: http://localhost:5173
      Cors__AllowedOrigins__1: http://localhost:8088
      AUTH_ADMIN_EMAIL: ${AUTH_ADMIN_EMAIL}
      AUTH_ADMIN_PASSWORD: ${AUTH_ADMIN_PASSWORD}
      AUTH_ADMIN_FULL_NAME: ${AUTH_ADMIN_FULL_NAME}
      AUTH_ADMIN_FORCE_PASSWORD_RESET: ${AUTH_ADMIN_FORCE_PASSWORD_RESET}
    ports:
      - "6003:8080"
    depends_on:
      identitydb:
        condition: service_healthy

  orders.api:
    image: orders-api
    build:
      context: .
      dockerfile: Orders/Orders.API/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_HTTP_PORTS: 8080
      MongoDb__ConnectionString: ${MONGODB_CONNECTION_STRING}
      MongoDb__DatabaseName: ${MONGODB_DATABASE_NAME:-EshopOrders}
      MongoDb__OrdersCollection: ${MONGODB_ORDERS_COLLECTION:-orders}
      Orders__TaxRate: ${ORDERS_TAX_RATE:-0.18}
      Jwt__Issuer: ${JWT_ISSUER:-eshop.identity}
      Jwt__Audience: ${JWT_AUDIENCE:-eshop.apis}
      Jwt__Key: ${JWT_KEY}
      BasketApi__BaseAddress: http://basket.api:8080
      CatalogApi__BaseAddress: http://catalog.api:8080
      Cors__AllowedOrigins__0: http://localhost:5173
      Cors__AllowedOrigins__1: http://localhost:8088
      Cors__AllowedOrigins__2: ${NETLIFY_ORIGIN:-http://localhost:5173}
    ports:
      - "6004:8080"
    depends_on:
      basket.api:
        condition: service_started
      catalog.api:
        condition: service_started
